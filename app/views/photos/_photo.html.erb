<%# Паршал для вывода отдельной фотографии %>
<div class="event-photo buffer-both">
  <% unless photo.photo.thumb.blank? %>
    <%= link_to image_tag(photo.photo.thumb.url), photo.photo.url unless photo.photo.thumb.blank? %>

  <%# Показываем не только фотку, но и юзeра, который её добавил %>
    Добавил <%= render partial: 'users/user', object: photo.user %>
  <% end %>
</div>
